name: windows
channels:
  - conda-forge
  - defaults
dependencies:
  - tbb-devel>=2021
  - sundials==6.1.1
  - cmdstan==2.32.1 # for speed, we don't really need all the mingw stuff this pulls in
  - pip
